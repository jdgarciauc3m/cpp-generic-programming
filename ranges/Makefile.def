RANGES_DIR=ranges
RANGES_PARTS=$(RANGES_DIR)/*.tex

include $(RANGES_DIR)/intro/Makefile.def

RANGES_BASE=ranges
RANGES_PDF=$(RANGES_DIR).pdf
FINAL_RANGES_PDF=$(FINAL_DIR)/$(RANGES_PDF)

ALL_PARTS += \
$(RANGES_PARTS)

ALL_SLIDES_FINAL_PDF += \
$(FINAL_DIR)/$(RANGES_PDF) \

