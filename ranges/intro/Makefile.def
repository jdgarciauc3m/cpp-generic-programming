RANGES_INTRO_BASE=intro
RANGES_INTRO_DIR=$(RANGES_DIR)/$(RANGES_INTRO_BASE)
RANGES_INTRO_PARTS=$(RANGES_INTRO_DIR)/*.tex
RANGES_PARTS += $(RANGES_INTRO_PARTS)

RANGES_INTRO_PDF=$(RANGES_DIR)-$(RANGES_INTRO_BASE).pdf

FINAL_RANGES_INTRO_PDF=$(FINAL_DIR)/$(RANGES_INTRO_PDF)

ALL_SLIDES_FINAL_PDF += \
$(FINAL_DIR)/$(RANGES_INTRO_PDF) \
